import XCTest
@testable import Leaferentiate

final class LeaferentiateTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Leaferentiate().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
